# Frontend

This directory contains the lightweight web UI for RegBrain:

- **Chat interface** — simple form to submit regulatory questions
- **Answer display** — renders the generated answer with inline citations
- **Eval dashboard** — shows RAGAS scores (faithfulness, relevancy, etc.) alongside answers
- **Static assets** — HTML/CSS/JS served by FastAPI or a simple dev server
